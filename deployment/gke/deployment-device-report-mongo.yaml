apiVersion: v1
kind: Service
metadata:
  name: device-report-mongo
spec:
  ports:
  - port: 27017
    name: device-report-mongo
  selector:
    app: device-report-mongo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: device-report-mongo-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
---
apiVersion: apps/v1beta2 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: device-report-mongo
spec:
  selector:
    matchLabels:
      app: device-report-mongo
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: device-report-mongo
    spec:
      containers:
      - image: mongo:3.7-jessie
        name: device-report-mongo        
        ports:
        - containerPort: 27017
          name: device-report-mongo
        volumeMounts:
        - name: device-report-mongo-persistent-storage
          mountPath: /data/db
      volumes:
      - name: device-report-mongo-persistent-storage
        persistentVolumeClaim:
          claimName: device-report-mongo-pv-claim
